name: Actions
on: [push, workflow_dispatch]
jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: actions/checkout@v4
            - name: install dependencies
              run: npm ci
            - name: Run lint
              run: npm run lint
            - name: Run Tests
              run: npm test
            - name: Build
              run: npm build
            - name: Deploying
              run: echo "Deploying....."